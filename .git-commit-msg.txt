feat: Complete three new features - voltage scaling, sensor registry, diagnostics

Voltage Range Scaling (CalibrationHelper):
- LinearPreset/NtcPreset structs extended with minVoltage/maxVoltage
- New methods: calculateLinearValueScaled(), normalizeVoltage(),
  getPresetMinVoltage(), getPresetMaxVoltage()
- Real-world voltage ranges for all 12 linear + 6 NTC presets
- QML: Min V / Max V display columns in AnalogInputs and ExBoardAnalog
- JS math moved to C++: expanderChannelMultiplier(), frequencyDividerForCylinders()

SensorRegistry (Core/SensorRegistry.h/.cpp):
- Runtime sensor availability tracking
- Sources: CAN, AnalogInput, DigitalInput, ExpanderBoard, GPS, SenseHat, Computed
- ~35 common CAN sensors pre-registered (activate on data receipt)
- 9 built-in GPS/SenseHat sensors always active
- Refresh methods for analog/digital/expander configuration changes
- Exposed to QML as "SensorRegistry" context property

DiagnosticsProvider + Diagnostics Page:
- Core/DiagnosticsProvider.h/.cpp: System info, CAN metrics, connection status, log buffer
- PowerTune/Settings/DiagnosticsSettings.qml: 2x2 grid layout
  - System panel: CPU temp, memory, uptime
  - Connection panel: CAN status, daemon, message rate, errors
  - Sensor panel: Active sensor table with 2s refresh
  - Log panel: Dark console with monospace green text
- New "Diagnostics" tab (11th) in SerialSettings.qml
