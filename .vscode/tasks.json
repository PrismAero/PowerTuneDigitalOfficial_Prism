{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build (Debug)",
            "type": "shell",
            "command": "cmake --preset macos-homebrew && cmake --build --preset macos-homebrew",
            "windows": {
                "command": "$vs = & \"${env:ProgramFiles(x86)}\\Microsoft Visual Studio\\Installer\\vswhere.exe\" -products * -latest -property installationPath; & \"$vs\\Common7\\Tools\\Launch-VsDevShell.ps1\" -Arch amd64 -HostArch amd64; cmake --preset windows-debug; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cmake --build --preset windows-debug"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            }
        },
        {
            "label": "Build (Release)",
            "type": "shell",
            "command": "cmake -S . -B build/release -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH=/opt/homebrew && cmake --build build/release --parallel",
            "windows": {
                "command": "$vs = & \"${env:ProgramFiles(x86)}\\Microsoft Visual Studio\\Installer\\vswhere.exe\" -products * -latest -property installationPath; & \"$vs\\Common7\\Tools\\Launch-VsDevShell.ps1\" -Arch amd64 -HostArch amd64; cmake --preset windows-release; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cmake --build --preset windows-release"
            },
            "group": "build",
            "problemMatcher": ["$gcc"],
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            }
        },
        {
            "label": "Clean",
            "type": "shell",
            "command": "rm -rf build/macos-homebrew build/release build/macos-dev",
            "windows": {
                "command": "if (Test-Path build/windows-debug) { Remove-Item -Recurse -Force build/windows-debug }; if (Test-Path build/windows-release) { Remove-Item -Recurse -Force build/windows-release }; if (Test-Path build/macos-homebrew) { Remove-Item -Recurse -Force build/macos-homebrew }; if (Test-Path build/release) { Remove-Item -Recurse -Force build/release }; if (Test-Path build/macos-dev) { Remove-Item -Recurse -Force build/macos-dev }"
            },
            "group": "build",
            "problemMatcher": [],
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            }
        },
        {
            "label": "Run",
            "type": "shell",
            "command": "./build/macos-homebrew/PowerTuneQMLGui.app/Contents/MacOS/PowerTuneQMLGui",
            "windows": {
                "command": "& \"${workspaceFolder}/build/windows-debug/PowerTuneQMLGui.exe\""
            },
            "group": "test",
            "problemMatcher": [],
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "dedicated"
            },
            "dependsOn": "Build (Debug)"
        },
        {
            "label": "Rebuild",
            "dependsOn": ["Clean", "Build (Debug)"],
            "dependsOrder": "sequence",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
